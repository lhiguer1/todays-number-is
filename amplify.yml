version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
